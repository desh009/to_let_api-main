{
  "name": "to-let-api",
  "version": "1.0.0",
  "private": true,
  "description": "Secure REST API for the To-Let Flutter application.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "check": "node --check api/index.js && node --check src/server.js && node --check src/app.js && node --check src/config/supabase.js && node --check src/middleware/auth.js && node --check src/routes/auth.js && node --check src/routes/listings.js && node --check src/schemas/auth.js && node --check src/schemas/listing.js",
    "test:email-sms": "node test-email-sms.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.115.0",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.1.0",
    "morgan": "^1.10.1",
    "nodemailer": "^10.0.9",
    "twilio": "^6.1.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
